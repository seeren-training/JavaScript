let monLet = "Mon let";
const maString = "Hello";
const maStringSimple = 'Hello';
const maStringTemplate = `Hello`;
const monBool = true;
const monNull = null;
const monNumber = 10.4;
const monArray = [
    "crer",
    "lister"
];

// Condition
if ("John" === "Doe" || "Doe" !== "Bill") {
} else if ("John" !== "Doe" && "Doe" !== "Bill") {
} else {
}

// Boucle
for (let i = 0; index < monArray.length; i++) {
}

// Point n'est plus l'opérateur de concatenation
console.log("Hello" + "World");
// Le point est un opérateur pour accéder à des propriétés
// count($monArray) est remplacé par monArray.length
